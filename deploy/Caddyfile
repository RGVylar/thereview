thereview.example.com {
	# SvelteKit frontend (static build)
	root * /opt/thereview/frontend/build
	file_server

	# API proxy
	handle /api/* {
		reverse_proxy localhost:8000
	}
	handle /health {
		reverse_proxy localhost:8000
	}

	# SPA fallback
	try_files {path} /index.html
}
